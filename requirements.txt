# Python学习项目依赖包
# 本文件列出了运行项目示例代码所需的第三方包

# Web框架示例用 (projects/simple_web_app.py)
flask==2.3.3

# 数据处理示例用 (可选)
pandas==2.0.3
numpy==1.24.3

# HTTP请求示例用 (可选)
requests==2.31.0

# 图像处理示例用 (可选)
pillow==10.0.0

# 测试框架 (可选)
pytest==7.4.0

# 代码格式化 (开发用)
black==23.7.0
flake8==6.0.0

# 虚拟环境管理 (可选)
virtualenv==20.24.0

# 注意：
# 1. 大部分示例代码只使用标准库，不需要额外安装
# 2. Web应用示例需要Flask
# 3. 可以根据需要安装相应的包
# 4. 建议使用虚拟环境来管理依赖
